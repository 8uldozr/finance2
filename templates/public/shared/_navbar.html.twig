<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="{{ path('public_home') }}">Logo</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
            <span class="visually-hidden">(current)</span>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Analyses</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="#">SP500</a>
            <a class="dropdown-item" href="#">Nasdaq</a>
            <a class="dropdown-item" href="#">Dow jones</a>
            <a class="dropdown-item" href="#">Russell</a>
          </div>
        </li>
        <li class="nav-item">
        <li class="nav-item">
          <a class="nav-link" href="#">Nous contacter</a>
        </li>

        {% if app.user %}
            {% if app.user.roles[0] is same as "ROLE_AUTHOR" %}
          <a class="nav-link" href="{{ path('public_home') }}">Se déconnecter</a>
            {% endif %}

        {% else %}
            
          <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
          
        {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>